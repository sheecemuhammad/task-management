$kind: http-request
url: "{{baseUrl}}/teams/{{teamBId}}/task-groups/{{groupId}}/tasks/{{taskId}}/comments"
method: POST
body:
  type: json
  content: "{\r

    \  \"content\": \"Testing real-time comment creation\"\r

    }"
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
order: 1788879137522
