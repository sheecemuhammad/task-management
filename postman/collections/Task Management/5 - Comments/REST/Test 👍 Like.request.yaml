$kind: http-request
url: "{{baseUrl}}/teams/{{teamBId}}/task-groups/{{groupId}}/tasks/{{taskId}}/comments/{{commentId}}/like"
method: POST
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
order: 4497909687734737
