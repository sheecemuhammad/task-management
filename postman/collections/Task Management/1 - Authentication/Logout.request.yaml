$kind: http-request
url: "{{baseUrl}}/auth/logout"
method: POST
body:
  type: json
  content: "{\r

    \  \"refreshToken\": \"{{refreshToken}}\"\r

    }"
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
order: 4412715874507015
