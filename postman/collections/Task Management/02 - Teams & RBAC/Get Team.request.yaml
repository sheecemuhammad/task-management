$kind: http-request
name: Get Team
url: "{{baseUrl}}/teams/{{teamId}}"
method: GET
body:
  type: json
  content: ""
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
order: 1200490927263183
