$kind: http-request
url: "{{baseUrl}}/teams/{{teamBId}}/task-groups/{{groupId}}/tasks"
method: POST
body:
  type: json
  content: "{\r

    \  \"title\": \"Test Team Isolation\"\r

    }"
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
order: 4503176248357219
